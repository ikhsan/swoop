Swift objective-C comparison report (swoop)
==

[x] get appropriate files from specified folder (xcodeproj)
  [x] path validations
    [x] check if project path exists
    [x] default values for folders
    [x] raise exception when xcodeproj is missing or nil
  [x] add real xcode project as a fixture
  [x] use path like `classes/models`
[] analyse all files (sourcekitten)
  [x] count protocol, classes, struct and categories for each languages
  [x] process data, called it as a 'report snapshot'
  [] objective-c ???
[] collate through time (git)
  [] get current branch
  [] collect commit's hashes for checkpoints
    [x] get latest n tags
    [] get latest n weeks
[] renderer
  [] default to terminal only (show as table?)
  [] create self-contained html report for interactivity
  [] csv
